import styled from "styled-components";

export const Title = styled.h2`
  font-family: Caveat;
  font-style: normal;
  font-weight: bold;
  font-size: 80px;
  line-height: 38px;
  color: #3f454a;
  z-index: 2;
`;
